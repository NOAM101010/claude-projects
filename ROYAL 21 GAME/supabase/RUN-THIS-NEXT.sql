-- =============================================================================
-- ROYAL 21 — שלב K — הסרת גביע פודיום
--
-- הרץ פעם אחת ב-Supabase → SQL Editor. Select All → Paste → Run. Idempotent.
--
-- מה זה עושה: מסיר את גביע האירוע `ev_weekly_winner` מקטלוג ההישגים. הפרס
-- הכספי של הפודיום השבועי נשאר כמו שהוא — רק הגביע יורד (מ-9 גביעים ל-8).
-- =============================================================================

delete from public.achievements where id = 'ev_weekly_winner';

-- ============================ סוף — הכל רץ ✓ ================================
