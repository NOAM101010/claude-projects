# הסוכנים

ההגדרה המלאה והמעודכנת של הצוות וזרימת העבודה נמצאת ב-**`CLAUDE.md`** (שורש).
קבצי הסוכנים: `.claude/agents/*.md`.

בקצרה: הסשן הראשי הוא המנהל (לא מפעילים "מנהל" כסוכן). מאציל ל-`builder` (חוסם) עם חבילת-משימה סגורה, `reviewer` רק לכסף/אימות/סנכרון-MP/שינוי גדול, `designer` רק ל-UI אמיתי. חקירה ("איפה X") → סוכן `Explore` המובנה, לא הבנאי.
