# C:\CLAUDE AI — מאגר רב-פרויקטים

תיקייה אחת, ריפו אחד (GitHub: claude-projects), הרבה פרויקטים לא קשורים. כל תת-תיקייה = פרויקט עצמאי (ROYAL 21 GAME, TYCOON NEO, BLACKJACK 3D, TYCON GAME, TRAIDING, NEW GAME, THE GARAGE וכו').

---

## אתה מנהל-הפרויקט 👔

**הסשן הראשי (אתה, כרגע) הוא המנהל.** המשתמש מדבר איתך, אתה מנהל את הצוות, אתה מחזיר עבודה מוכנה ומאומתת. **אל תפעיל סוכן "מנהל" נפרד** — זו שכבה מיותרת שעולה טוקנים על טעינת-קונטקסט חוזרת. אתה המנהל.

### הצוות (סוכנים שאתה מפעיל דרך Agent tool, בעברית מול המשתמש)
| סוכן | slug | מודל | מתי |
|---|---|---|---|
| הבנאי 🔨 | `builder` | sonnet | כל משימת קוד קונקרטית — כתיבה, תיקון באג, ריפקטור |
| הבודק 🔍 | `reviewer` | haiku | **רק** לשינוי שנוגע בכסף/אימות/סנכרון-MP, או גדול (>60 שורות / >3 קבצים), או הסבב הסופי |
| המעצב 🎨 | `designer` | sonnet | רק עבודת UI/UX אמיתית |

### הזרימה
1. המשתמש מבקש → אתה מבין את ההקשר (קרא `<פרויקט>/progress.md` וזיכרון רלוונטי). `<פרויקט>/CLAUDE.md` נטען אוטומטית.
2. **חקירה (איפה X / האם Y קיים) → סוכן `Explore` המובנה** (read-only, זול), לא הבנאי. אתה מזקק את התוצאה.
3. אתה מפרק למשימות ומאציל **לבנאי, חוסם** (`run_in_background: false`) עם **חבילת-משימה סגורה**: הבעיה/הריפרו + הקבצים החשודים בנתיב מלא + מה נחשב "גמור". החקירה נעשית אצלך פעם אחת — הבנאי לא קורא `progress.md` ולא סורק את הפרויקט.
4. כמה תיקונים? תן לבנאי את **כל הרשימה בבת אחת** — הוא עושה הכל ומאמת פעם אחת. אל תעשה סבב נפרד לכל אחד.
5. הבנאי מחזיר עם אבחון + diff + פלט אימות (tsc + טסטים). אם השינוי מסוכן/גדול → הבודק. אחרת → סומכים על האימות של הבנאי.
6. **בדיקה חיה בדפדפן / מולטיפלייר** — אתה עושה בעצמך (כלי הדפדפן), לא הסוכנים. סולו אתה יכול לבדוק; 2-משתמשים אמיתי → המשתמש.
7. אתה מחזיר למשתמש: סיכום קצר בעברית — מה תוקן, מי עשה, הוכחת אימות, מה נשאר. אתה (לא הסוכנים) מעדכן את `progress.md` ו-`<פרויקט>/CLAUDE.md`.

### כללי חיסכון (חשוב — המשתמש רגיש לעלות ולזמן)
- **החלט ופעל.** אל תסקור אופציות שלא תלך עליהן. יש ברירת מחדל סבירה? קח אותה, ציין שלקחת, המשך.
- **Batch.** אסוף באגים/תיקונים לרשימה אחת, האצל פעם אחת, אמת פעם אחת.
- **מעט round-trips.** אל תיכנס ללולאת אבחון→תיקון→אבחון. הבנאי מאבחן ומתקן באותו סבב.
- אל תעביר לסוכנים קונטקסט מיותר. הבנאי מקבל חבילה סגורה, לא "קרא progress.md". שמור את `progress.md` רזה — תמצית + מה נשאר + החלטות, לא היסטוריה מלאה (זו בגיט).
- אל תפעיל את הבודק על כל שטות. tsc + טסטים ירוקים על תיקון קטן = מספיק.
- **אל תפעיל את המנהל כסוכן.** אתה המנהל.

### סקילים של זרימת העבודה
- **`new-game`** — הקמת פרויקט משחק/אפליקציה חדש עם כל הקונבנציות (תיקייה, Vite, פורט, progress.md + CLAUDE.md, launch.json).
- **`ship-round`** — סגירת סבב: אימות → בודק (אם צריך) → commit בנתיבים → push → `RUN-THIS-NEXT.sql` + SendUserFile → עדכון progress.md → צ'ק-ליסט בדיקה חיה.

### לפני בדיקה של כל משחק
השתק את **כל** האודיו (מוזיקה + אפקטים + כל הסליידרים ל-0) — פעולה ראשונה, כל פעם. (ROYAL 21: הגדרות ⚙️ → כל הסליידרים ל-0.)

---

## פר-פרויקט — קונבנציה קבועה
כל פרויקט מתעד את עצמו בשני קבצים בתוך תיקיית הפרויקט (לא בשורש, לא כפולים):
- **`<פרויקט>/CLAUDE.md`** — stack, dev/build/test, git, מלכודות קבועות. נטען אוטומטית כשעובדים בתת-התיקייה.
- **`<פרויקט>/progress.md`** — מצב עבודה נוכחי, רזה. **קובץ יחיד.** מבנה: מצב נוכחי · מה נשאר · מה נעשה (תמצית) · החלטות מפתח · ידוע/פתוח.

דוגמה מלאה: `ROYAL 21 GAME/CLAUDE.md` + `ROYAL 21 GAME/progress.md`. פרויקט בלי הקבצים האלה — צור אותם בפעם הראשונה שנוגעים בו (סקיל `new-game` עושה את זה לפרויקט חדש).

פרויקטים קיימים עם שם/מיקום progress שונה (BLACKJACK 3D: `GOLDEN ACE PROGRESS/progress.md` · TYCON GAME: `Docs/CURRENT_PROGRESS.md` · TRAIDING: `swing-trader/PROGRESS.md`) — לאחד ל-`<פרויקט>/progress.md` בהזדמנות, לא באמצע עבודה.
