# ROYAL 21 — מסמך המשך

**מה זה:** משחק קזינו חברתי בצ'יפים וירטואליים בלבד. Vite + React 18 + TypeScript + Supabase.
**איך להמשיך:** פתח צ'אט חדש בתיקייה `C:\CLAUDE AI`, ותן לו את הקובץ הזה. הוא יבין מאיפה להמשיך.

---

## פקודות

```bash
npm run dev
```

```bash
npm run build
```

```bash
npm run test:all
```

מצב נוכחי: `tsc` נקי, build עובר, 49 בדיקות — פוקר (טורניר, time bank, equity) + מנוע/מכונת מזל/מוזיקה + `test:social` (VIP, streak, gift) — כולן עוברות (`npm run test:all`).

---

## מבנה

```
src/
  app/          App.tsx (MotionConfig, אודיו, presence) · routes.tsx
  audio/        AudioManager.ts — הכל מסונתז, אפס קבצי שמע
  components/
    layout/     HUD · SideNav · BackButton · SceneShell · ChipsPanel
    social/     ChatPanel · FriendsPanel · GiftModal · InviteOverlay · Avatar
    ui/         GameButton (forwardRef) · Tooltip · Modal · Onboarding · VipBadge · StreakBadge
  data/         items.ts · achievements.ts · economy.ts (VIP tiers, streak, gift limit) · slots.ts
  games/        blackjack/ (engine+duel) · slots/ · scratch/ · coinflip/ · highcard/ · night/
  scenes/       hub · room · night · blackjack · vault · profile · settings · auth · intro
  services/     supabase (db, isRemoteId) · auth · profile (milestones) · rooms · shop · gift · chat · accounts
  stores/       usePlayer · useRoom · useSocial · useSettings · useUI
supabase/       setup.sql (בסיס) · upgrade.sql (הרץ אחריו)
scripts/        engine.test.ts · slots.test.ts · audio.test.ts · poker.test.ts · social.test.ts
```

---

## מה כבר נעשה

**באגים שתוקנו**
- כרטיס גירוד: הקנבס נמדד תחת `scale(0.9)` של אנימציית הכניסה → המברשת הוסטה והסף לא היה בר-השגה. עבר ל-`ResizeObserver` + `offsetWidth`.
- כרטיס גירוד שני: חסר `key` ל-`ScratchCard`, הרכיב מוחזר עם `done=true`. נוסף `ticket` לכל כרטיס.
- `#boot-veil`: הוסר דרך `requestAnimationFrame` שלא רץ בטאב רקע → מסך שחור שבולע קליקים. נוסף fallback בטיימר.
- אודיו: `unlock()` סימן הצלחה גם בכשל, ומאזינים היו `{once:true}`. עכשיו מרפא-עצמי.
- מוזיקה: היו 3 אוסצילטורים שמחזיקים אקורד לנצח (זמזום). הוחלף במתזמן lookahead עם לולאת Am7–Dm7–G7–Cmaj7.
- Hub: `.social-zone`/`.profile-zone` היו absolute עם `translateX(±100%)` → חפיפה. הוחלף ברשת רספונסיבית.
- 400 בלולאה: מזהי אורח מקומיים נשלחו ל-Supabase. נוסף `isRemoteId()` בכל השירותים.
- "אנימציות מופחתות" לא השפיע על framer — נוסף `MotionConfig`.
- חנות: כשל שקט (`if (ok)` בלי `else`). עכשיו כל כשל מוצג עם סיבה.
- **זהות**: `authService.restore()` נפל בשקט לפרופיל המקומי כשלא הייתה שורת `profiles`, ואז כל RLS דחה (403/409). עכשיו יוצר את השורה ומאמץ את זהות ה-session.

**נוסף**
- תפריט צד, כפתור "חזרה לחדר" בכל מסך, טולטיפים כלליים
- מכונת מזל מלאה (RTP מדוד 88.75%)
- ערב חברה `/night/:code` עם לוח ניקוד והזמנת חברים
- צ'אט חדר עם היסטוריה נשמרת ומגבלת קצב בשרת
- מדריך פתיחה בן 6 שלבים, ניתן להרצה חוזרת מההגדרות
- 27 הישגים ב-4 דרגות (7 חברתיים) → גביעים על מדף ב"החדר שלי"
- החלפת משתמש + זיכרון חשבונות במכשיר
- מעקב יריבות ראש-בראש (`usePlayer.recordRivalry`)

---

## מה נשאר — לפי סדר עדיפות

### 1. תצוגת יריבות + טבלת שיאים בין חברים — ✅ נעשה
`MyRoomScene` עכשיו קורא מ-`usePlayer.rivalries` האמיתי (לא עוד `stats.wins` מול `view.wins`), ויש פאנל "יריבויות מול חברים" שמראה טבלה מלאה מול כל חבר ששיחקת נגדו. `recordRivalry` מקבל עכשיו את שם המשחק (`blackjack` / `poker`) כדי שהיריבות תיספר נכון לפי משחק.

### 2. תחרות בסבבים על כל המשחקים — עדיין לא (לפי בקשה מפורשת, לא בסקופ הנוכחי)

### 3. שולחן פוקר — ✅ נעשה
Texas Hold'em מלא: `src/games/poker/` (מנוע + דירוג ידיים + מבחנים), `pokerService.ts` + `usePokerRoom.ts` (אותו דגם host-authoritative כמו Blackjack), `PokerScene.tsx`, כרטיס בלובי הראשי. עד 6 שחקנים אמיתיים (בלי בוטים), blinds, סבבי הימור מלאים, side pots, showdown. `npm run test:poker` (כלול ב-`test:all`) מוודא זוגיות צ'יפים (zero-sum) ודירוג ידיים נכון.
**דורש הרצת `supabase/poker.sql`** (מרחיב את מגבלת המושבים מ-4 ל-6) — ראה למטה.

### 4. Sit & Go טורנירים — ✅ נעשה
`SitAndGoScene.tsx` + `useSngRoom.ts` + `sngService.ts` (עותק דק של תשתית הפוקר הרגיל, אותו `rooms.state`, אין צורך ב-SQL נוסף). Buy-in קבוע שהמארח בוחר (500–10,000), כולם מתחילים עם 1,500 צ'יפים טורניר, בליינדים עולים כל 5 דקות לפי מבנה סטנדרטי (12 שלבים, אנטה מתחיל בשלב 5), אין re-buy, אלימינציה כשהסטאק מגיע ל-0, המנצח לוקח את כל קופת ה-buy-in. הידיים מתחילות אוטומטית — אין כפתור "יד הבאה". נבדק ב-`npm run test:poker`: זוגיות צ'יפים גם עם אנטה+side pots, escalation לפי שעון אמיתי, וניצחון יחיד בסוף.

### 5. Auto-Play Slots — ✅ נעשה
10/25/50 סיבובים אוטומטיים עם טיימר בין כל סיבוב, עצירה אוטומטית עם יעדר צ'יפים. נוכחי ב-`SlotsScene.tsx`.

### 6. Time Bank בפוקר (וב-Sit & Go) — ✅ נעשה
30 שניות להחלטה, 2 בנקים של 60 שניות כל אחד לכל שחקן (לא מתחדשים), timeout אוטומטי = fold. הטיימר מוצג ב-UI (פס התקדמות + מספר שניות), המארח (host) אחראי לזהות timeout ולשלוח את הפעולה.

### 6. Detailed Statistics לכל חבר — ✅ נעשה
פאנל היריבות בפרופיל (`MyRoomScene`) מראה עכשיו פירוט **לפי משחק** (Blackjack / Poker / וכו') מול כל חבר — לא רק סך הכול. הנתון כבר נאסף (`rivalries.byGame`), רק לא הוצג עד עכשיו.

### 7. Spectate Mode — ✅ נעשה (עם הגבלה אחת)
בפוקר (כולל Sit & Go) — צפייה אמיתית בלי לתפוס מושב, ותג "👁 N" שמראה לשחקנים כמה צופים יש. ב-Blackjack מול חברים — אותו תג "👁 N", אבל הוא סופר שחקנים שכבר בחדר ומחוץ ליד הנוכחית (לא צופים שמעולם לא נכנסו), כי כניסה לחדר Blackjack כבר תופסת מושב ב-DB. לפתוח את זה במלואו דורש להפריד "חברות בחדר" מ"מושב במשחק" — שינוי ארכיטקטוני גדול יותר, לא בסקופ הנוכחי.

### 8. Achievements חדשים + Cosmetics + רקע אינטראקטיבי — ✅ נעשה
5 הישגים חדשים (Poker Millionaire, Streak Master, All-In Legend, Royal Flush, Sit & Go Champion — כולל חישוב equity אמיתי ל-"All-In Legend", לא ניחוש). קוסמטיקה חדשה בחנות: 5 מטבעות ל-Coin Flip, 5 ערכות נושא למכונת המזל (ויזואלי בלבד, לא משפיע על סיכויים/תשלומים — `sl_classic` ו-`cn_classic` בחינם וכבר בבעלות כולם, השאר נרכשים). אנימציית הפריט האגדי/מיתי בחנות תוקנה (לא עוד מסתובבת מחוץ לקופסה — עכשיו glow+shimmer מוכל). רקע אתר עם צ'יפים/קלפים צפים ואור שעוקב אחרי העכבר (`AmbientBackground.tsx`), מכבד "תנועה מופחתת".
**דורש הרצת `supabase/setup.sql` מחדש** (אידמפוטנטי — upsert) כדי שהפריטים החדשים יהיו קיימים בטבלת `items` בשרת, אחרת רכישה שלהם תיכשל.

### 9. Gift System — ✅ נעשה
שליחת צ'יפים לחבר, עד 500/יום (נאכף בשרת ב-`send_gift()` — סיכום כל המתנות שנשלחו היום, לא מונה שיכול לסטות). כפתור 🎁 ליד כל חבר מחובר בפאנל החברים (`GiftModal.tsx`), עם סכומים מהירים ותצוגת "נשאר X מתוך 500". הודעה מגיעה לנמען בזמן אמת (טבלת `notifications`, קטגוריה חדשה `'gift'`) — הצ'יפים מזוכים מקומית מיד אם הוא מחובר, ולא מחכים לרענון הבא. שחקנים מקומיים/אורחים לא רואים את הכפתור (`isRemoteId`).
**דורש `supabase/setup.sql`**: טבלת `chip_gifts` חדשה + RLS + פונקציית `send_gift()`.

### 10. VIP Levels — ✅ נעשה
לא מערכת XP נפרדת — נבנה ישירות על גבי הרמה/XP הקיימים. 3 דרגות (`VIP_TIERS` ב-`economy.ts`): רמה 1+ / 16+ / 36+, עם הנחה בחנות של 5% / 10% / 15% (נאכפת בשרת בתוך `buy_item()`, לא רק בתצוגה). כל 5 רמות מקבלים תגמול אבן-דרך אוטומטי — תמיד צ'יפים, ולפעמים (הסיכוי עולה לפי הדרגה) גם פריט קוסמטי נדיר שעוד אין ברשותכם. נראה ב-HUD (`VipBadge.tsx`) ובפאנל ייעודי ב"החדר שלי" (הדרגה, ההנחה, וכמה רמות נשארו לתגמול הבא).
**דורש `supabase/setup.sql`**: עמודת `last_milestone_claimed` על `profiles`, פונקציית `claim_level_milestone()`, ושינוי ל-`buy_item()` שמחיל את ההנחה.

### 11. Login Streak — ✅ נעשה (מחליף את המתנה היומית)
מה שהיה "מתנה יומית" עם מחזור קבוע של 7 ימים (בלי קשר אם פספסת יום) הפך לרצף אמיתי: מפספסים יום — חוזרים ליום 1. תגמול: ימים 1–3 = 50 צ'יפים, 4–7 = 100, 8+ = 250. אותו אייקון 🎁 ב-Hub, עכשיו עם 🔥 ומספר הימים ב-HUD ובכרטיס. לוקאלי בלבד (כמו קודם) — אין צורך בשינוי DB.

### 12. עיצוב "החדר שלי" — ✅ נעשה
שתי קטגוריות חדשות בחנות: `backgrounds` (5 רקעים ל"חדר שלי", גרדיאנטים בלבד — `src/data/roomThemes.ts`) ו-`decor` (6 פריטי דקורציה: מדף גביעים, עציץ, שלט ניאון, שעון קיר, אח בוערת, נברשת קריסטל). רקע מתחלף כמו כל קוסמטיקה אחרת (`equip()` הגנרי הקיים). דקורציה שונה — עד 4 פריטים בו-זמנית, מכפתור "הוסף/הסר מהחדר" ייעודי (`toggleDecor()` ב-`usePlayer.ts`), לא "השתמש" רגיל. פאנל חדש "עיצוב החדר" ב"החדר שלי" מציג את מה שמצויד.
**דורש `supabase/setup.sql`**: 11 הפריטים החדשים בזרע (`gen:seed` כבר רץ, הקובץ מעודכן).

### 13. תיקון גלילה בטאבי הקטגוריות — ✅ נעשה
`Tabs.tsx` (המשותף לחנות ולפאנל החברים) מקבל עכשיו גלגלת-עכבר → גלילה אופקית (`onWheel`), ומסכת דהייה קבועה בשני הקצוות שמראה מיידית שיש עוד קטגוריות לגלול אליהן. רלוונטי במיוחד עכשיו עם 17 קטגוריות בחנות.

### 14. לוח מנהיגים שבועי בין חברים — ✅ נעשה
טאב חדש "לוח מנהיגים" בפאנל החברים, מדורג לפי יתרת צ'יפים כוללת (חי, בלי snapshot — אין תשתית cron בפרויקט). כל שבוע השחקן המוביל מקבל 1,000 צ'יפים בפעם הראשונה שהוא פותח את הטאב אחרי 7 ימים; ה-RPC בודק בעצמו זכאות (כולל שיש לפחות חבר אחד — כדי שחשבון בלי חברים לא "ינצח" אוטומטית).
**דורש `supabase/setup.sql`**: עמודת `weekly_prize_claimed_at` על `profiles`, פונקציית `claim_weekly_prize()`.

### 15. בונוס חזרה (Comeback) — ✅ נעשה
מעל תגמול הרצף היומי הרגיל: מי שחוזר אחרי 3+ ימי היעדרות מקבל בונוס נוסף חד-פעמי של 300 צ'יפים, עם הודעה נפרדת "ברוך שובך!" (לא "רצף כניסה" הרגיל). לוקאלי בלבד, בונה על `daily.lastClaim` הקיים — אין שינוי DB.

### 16. איפוס סיסמה באימייל — ✅ נעשה
"שכחת סיסמה?" מופיע רק במצב התחברות. שולח קישור איפוס (`resetPasswordForEmail`, `redirectTo` חוזר ל-`/login`). הקישור חוזר עם session שכבר הוקם אוטומטית (`detectSessionInUrl` שכבר היה דלוק) ומפעיל אירוע `PASSWORD_RECOVERY` שהאפליקציה מאזינה לו — מעביר את המסך אוטומטית לטופס "קבע סיסמה חדשה". אין שינוי DB, אבל **דורש הגדרה ב-Supabase**: Authentication → Email Templates → Reset Password מוגדר (SMTP פעיל), וש-`<domain>/login` נמצא ברשימת ה-Redirect URLs המורשים.

---

## Supabase — חובה

1. הרץ `supabase/setup.sql` — **גם אם כבר רץ בעבר**, יש בו עכשיו: 10 פריטי קוסמטיקה (מטבעות + ערכות מכונת מזל) + 11 פריטי עיצוב חדר (רקעים + דקורציה), טבלת `chip_gifts` (מתנות), עמודות `last_milestone_claimed` ו-`weekly_prize_claimed_at` על `profiles`, ופונקציות `send_gift()` / `claim_level_milestone()` / `claim_weekly_prize()`. אידמפוטנטי, בטוח להריץ שוב.
2. הרץ `supabase/upgrade.sql` — מוסיף צ'אט, צופים, ו**משלים פרופילים למשתמשים שנרשמו לפני שהסכימה הותקנה**. אידמפוטנטי.
3. הרץ `supabase/admin.sql` — הופך את `noamshay1010@gmail.com` לאדמין (`/admin` בתפריט).
4. הרץ `supabase/confirm-existing-emails.sql` — משחרר חשבונות שנתקעו לפני "Confirm email".
5. הרץ `supabase/poker.sql` — מרחיב מושבי חדר ל-6 בשביל שולחן הפוקר.
6. **Authentication → Providers → Anonymous sign-ins → הפעל.** בלי זה חברים לא יכולים להיכנס דרך לינק, ואי אפשר לפתוח חדרים/צ'אט/חנות.
7. תחת Email — כבה **Confirm email** לבדיקות (או שהחשבון נשאר תקוע עד שמריצים את #4).
8. לאיפוס סיסמה: ודא ש-SMTP מוגדר (Authentication → Email Templates → Reset Password) וש-`<domain>/login` ברשימת ה-Redirect URLs המורשים (Authentication → URL Configuration).

בדקתי את קוד ההתחברות/צ'אט/חנות לעומק ולא מצאתי שם עוד באג — ה-401/403 שאתה רואה הם כמעט תמיד סימפטום של 1–2 שלבים שלא רצו עדיין (במיוחד #3–#4 ו-#6). אחרי שמריצים את כל השישה ומדליקים Anonymous sign-ins, "כניסה עם אמייל", "צ'אט" ו"חנות" אמורים לעבוד.

`.env` מכיל `VITE_SUPABASE_URL` ו-`VITE_SUPABASE_ANON_KEY`.

---

## כללי עבודה שכדאי לשמור

- הכל בצ'יפים וירטואליים. אין רכישה, הפקדה או משיכה בשום מקום.
- אודיו מסונתז בלבד — לא להוסיף קבצי שמע.
- עברית RTL דרך תכונות CSS לוגיות (`ms/me`, `inset-inline`).
- כל מפתח תרגום חייב להתקיים גם ב-`he.json` וגם ב-`en.json`.
- כל שירות שמקבל מזהה שחקן חייב לעבור דרך `isRemoteId()` לפני פנייה ל-Supabase.
